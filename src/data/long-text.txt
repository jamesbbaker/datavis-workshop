The Observable Framework turns markdown into a live site where data loaders run at build time and interactive charts update in the browser. You can attach zip archives split text into words with split and slice join the first few words with a slider. Scatter plots pair GDP with life expectancy on log and linear scales while color encodes a third metric. This paragraph exists so FileAttachment can load plain text for the word slider exercise.
